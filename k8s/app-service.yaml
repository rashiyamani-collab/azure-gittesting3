apiVersion: v1
kind: Service
metadata:
  name: demo-app2
  namespace: default
  labels:
    app: demo-app2
spec:
  type: LoadBalancer   # creates an AWS ELB in public subnets
  selector:
    app: demo-app2
  ports:
    - name: http
      port: 80
      targetPort: 80

